- [ ] shell history (ask Yosh)
- [ ] bash autocomplete
- [ ] install exa

```bash
# .bashrc
if [ -x "$(command -v exa)" ]; then
    alias ls='exa --icons'
    alias la='exa --long --all --group --icons'
    alias tree='exa --tree --icons'
fi
```
